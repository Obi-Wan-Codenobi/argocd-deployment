apiVersion: v1
kind: Service
metadata:
  name: dispatcherhq-api-service
  labels:
    app: dispatcherhq-api
spec:
  selector:
    app: dispatcherhq-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
